apiVersion: apps/v1
kind: Deployment
metadata:
  name: hr-service
  namespace: default
  labels:
    app: hr-service
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hr-service
  template:
    metadata:
      labels:
        app: hr-service
        component: backend
    spec:
      containers:
        - name: hr-service
          image: us-central1-docker.pkg.dev/erp-system-478020/konecta-erp/hr-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5005
              name: http
              protocol: TCP
          envFrom:
            - configMapRef:
                name: hr-service-config
          env:
            - name: ConnectionStrings__DefaultConnection
              value: "Server=sqlserver;Database=Konecta_HR;User Id=sa;Password=pa55w0rd!;TrustServerCertificate=True;"
            - name: RabbitMq__HostName
              value: "rabbitmq"
            - name: RabbitMq__Port
              value: "5672"
            - name: RabbitMq__UserName
              value: "guest"
            - name: RabbitMq__Password
              value: "guest"
            - name: ServiceConfig__PublicHost
              value: "hr-service"
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /system/health
              port: 5005
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /system/health
              port: 5005
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
